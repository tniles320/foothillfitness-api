{
  "functions": {
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run lint"
    ],
    "rewrites": [
      {
        "source": "api",
        "destination": "/api"
      },
      {
        "source": "api",
        "destination": "/admin"
      }
    ]
  }
}
